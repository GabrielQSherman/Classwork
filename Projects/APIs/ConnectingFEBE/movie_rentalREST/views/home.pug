html
    title Movie Rentals

    script
        include ../public/home-pug.js

    style
        include ../public/home-pug.css

    body    
        if isLoggedIn
            button#logoutBtn Logout
        else
            button#loginBtn Login
        
        if isAdmin
            button#adminPage Admin Page
            

        #allMovies
            each movieData in all_movies
                if movieData.inventory.available > 0
                    .Movie(id=movieData._id)
                        .displayMovie
                            h1=movieData.title
                            h3=movieData.release
                            img(src=movieData.img, alt=`${movieData.title} Poster IMG`)
                            a(href=movieData.imdb_link)=`${movieData.title} IMDB Page`
                        if isAdmin
                            .editMovie
                                - const currentYear = new Date().getFullYear();
                                form(onSubmit='return false')
                                    input(type='text', placeholder='Title', name='title')
                                    input(type='number', placeholder='Release Year', name='release', min=currentYear-100, max=currentYear+2)
                                    input(type='url', placeholder='Movie Poster URL', name='img')
                                    input(type='url', placeholder='IMDB Link', name='imdb_link')
                                    button.submitEdit Submit Changes
                            .adminBtns
                                button.deleteMovie Delete From Database
                                button.editMovieBtn Edit Movie Data
                        else 
                            button.rentMovie Rent This Movie
                            -// 1 NEWBUTTON: add a new button for rented
                            -// 1.5: ONLY the button needed is shown --> make it clear to the user if they are already renting
                            -// 2: modify current button to serve both purposes (backend modification possibily)
                            -// button.getMovie Get More DB Info


